
    <center><a class="zocial linkedin" href="/auth/linkedin">Update informatii folosind LinkedIn</a></center><br><br>
    <div class="container-narrow">
    	
	<div class="masthead">
        <div id="navbar-example" class="navbar navbar-static">
            <div class="navbar-inner">
            <a class="brand" href="">Alumni</a>
              <div class="container" style="width: auto;">
                <ul class="nav" role="navigation">
                  <li class="active"><a href="/home">Harta colegi</a></li>                  
              </div>
            </div>
          </div>
    </div>
      
    <div class="well">


    	<h4 class="muted">Vezi pe harta unde se afla colegii tai.</h4>
        <%= stylesheet_link_tag 'gmaps4rails' %>
        <%= gmaps4rails(@json) %><br>
        <form class="form-search cerereAdmitere">
	    <div class="navbar navbar-static">
	    	<div class="navbar-inner">
	    	
	    	<span class="filterTagAlumni">Grupa</span>
	    	<select class="selectAlumni span1">
						<%  User.select("DISTINCT group").each do |g| %>
					        <option> <%= g.group %> </option>
                                                <% end %>					
			</select>
	    	
			<span class="filterTagAlumni">Promotia</span>
	    	        <select class="selectAlumni span2">
						<%  User.select("DISTINCT promotion").each do |g| %>
						  <option> <%= g.promotion %></option>
                                                <% end %>
			</select>
	    	
			<span class="filterTagAlumni">Specializare</span>
	    	        <select class="selectAlumni span3">
				                <%  User.select("DISTINCT specialization").each do |g| %>
						  <option> <%= g.specialization %></option>
                                                <% end %>		
			</select>
	    	
	        <button type="submit" class="btn">Actualizare</button>
	        
	    	
            </div>
         </div>
	     </form><br>

		<h4>Lista colegi</h4>
		<table class="table table-striped alumni">
		  <tr>
			 <th>Poza</th>
			 <th>Prenume</th>
			 <th>Nume</th>
			 <th>Locatie</th>
			 <th>Email</th>
			</tr>
			
		        <% for user in @users %>
			<tr>
			  <% if user.imageurl.nil? %> 
			    <td><%= link_to image_tag("no_profile.jpeg", width:"50", height:"50", alt: "logo"), '/users/'+user.id.to_s  %></td>
                          <% else %>
                            <td><%= link_to image_tag(user.imageurl, width:"50", height:"50", alt: "logo"), '/users/'+user.id.to_s %></td>
			  <% end %>
			  <td><%= user.first_name %></td>
     			  <td><%= user.last_name %></td>
                          <% if Location.find_by_id(user.location_id) %>
			    <td><%= Location.find_by_id(user.location_id).name %></td>
                          <%else%>                          
                            <td> </td>
                          <%end %>
			  <td><%= user.email %></td>		    	  
			</tr>
			<% end %>
		      
		</table>
	</div>


</html>
